# Network
bind 0.0.0.0
port 6379

# Persistence
save 900 1
save 300 10
save 60 10000
dbfilename dump.rdb
dir /data

# AOF
appendonly yes
appendfsync everysec

# Memory
maxmemory 256mb
maxmemory-policy allkeys-lru

# Slow log
slowlog-log-slower-than 10000
slowlog-max-len 128
